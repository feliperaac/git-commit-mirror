# Conexão com o banco de dados Postgres usando as variáveis do Docker
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Configurações da EvolutionAPI
evolution.api.url=https://evo.feliperalab.com
evolution.api.key=69BFFA1A825D-4F4B-9A89-FCDF0D028A53
evolution.api.instance=feliperalab
evolution.api.group.id=120363427747965028@g.us